import styled from "styled-components";

export const Wrapper = styled.div`
    margin-top: auto;
`;

export const Content = styled.div`
    p {
        color: var(--green);
        position: absolute;
        bottom: 0;
        margin-left: 2em;
    }
`;